input {
  beats {
    port => 5044
  }
}

filter {
  # Add any filters here, such as grok, mutate, etc.
}

output {
  elasticsearch {
    hosts => ["elasticsearch:9200"]
    index => "your_index_name-%{+YYYY.MM.dd}"
  }
}
